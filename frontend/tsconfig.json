{
  // Frontend TypeScript configuration for LexOrbital FrontRing
  // Extends the base configuration and adds React/frontend-specific settings
  //
  // Visit https://aka.ms/tsconfig to read more about TypeScript configuration
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // ============================================================================
    // Frontend-Specific Overrides
    // ============================================================================

    // File Layout: Frontend-specific paths
    "rootDir": ".",
    "outDir": "./dist",

    // JSX: Use React 17+ automatic JSX transform
    // This eliminates the need to import React in every file
    "jsx": "react-jsx",

    // Allow importing TypeScript extensions
    // Required for some bundlers and development tools
    "allowImportingTsExtensions": true,

    // No emit: Let bundler (Vite) handle compilation
    // TypeScript only does type checking, not compilation
    "noEmit": true,

    // Module resolution: Use bundler resolution for frontend
    // This works best with modern bundlers like Vite, Webpack, etc.
    "moduleResolution": "bundler",

    // Path aliases: Use @ for src directory
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },

    // Types: Empty array means no automatic type inclusion
    // Types will be included based on imports (recommended for frontend)
    "types": []

    // Lib: Not specified - will use default based on target
    // Frontend bundlers typically handle polyfills

    // ============================================================================
    // Inherited from base (via extends):
    // - All strict checking options
    // - Code quality options
    // - Output options (though noEmit overrides some)
    // - Compatibility options
    // ============================================================================
  },
  "include": [
    "src/**/*",
    "src/versions/v1/components/tests/**/*",
    "scripts/**/*",
    "*.ts",
    "*.tsx"
  ],
  "exclude": ["node_modules", "dist", "vitest.config.ts"]
}
